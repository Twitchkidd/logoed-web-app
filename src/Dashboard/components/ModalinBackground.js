import styled from "styled-components";
import { grayGray } from "../../utilities";

export const ModalinBackground = styled.div`
  background: ${grayGray};
  display: flex;
  justify-content: center;
  align-items: center;
`;
