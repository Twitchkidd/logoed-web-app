import React, { Component } from "react";

export default class Thanks extends Component {
  render() {
    return (
      <div>
        Thanks for signing up! Here's where you'll see the advertizing bucks at
        work! And potentially cancel giving us advertizing bucks if we can't
        prove it! lol
      </div>
    );
  }
}
