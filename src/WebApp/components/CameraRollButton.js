import styled from "styled-components";
import { eigengrau } from "../../utilities";

const CameraRollButton = styled.button`
  display: inline-block;
  width: 64px;
  height: 64px;
  background: ${eigengrau};
`;

export default CameraRollButton;
