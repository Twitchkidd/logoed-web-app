import styled from "styled-components";
import { sans } from "../../utilities";

const ToSLink = styled.button`
  font-family: ${sans};
  font-size: 1em;
  line-height: 1.15em;
  border: none;
`;

export default ToSLink;
