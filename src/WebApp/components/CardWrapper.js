import styled from "styled-components";
import { elevation, grayGray } from "../../utilities";

const CardWrapper = styled.div`
  width: 384px;
  height: 384px;
  background: ${grayGray};
`;

export default CardWrapper;
